
# Playground

This folder holds small components, UI experiments, and mini-projects.

Guidelines:
- Keep each experiment self-contained in its own folder.
- Expose a single React component as the default export called `PlaygroundComponent`.
- Link new experiments from `src/pages/PlaygroundIndex.js`.
